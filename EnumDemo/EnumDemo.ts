// 数字枚举，可设置初始化值
enum Directory {
    Up=1,
    Down,
    Left,
    Right
}

// 字符串枚举
enum DirectoryString {
    Up = 'Up',
    Down = 'Down',
    Left = 'Left',
    Right = 'Right'
}

// Heterogeneous Enum
enum HeterogeneousEnum {
    Up = 1,
    Down = 'Down'
}

enum E { X }
enum E1 { X, Y, Z }